# pnpm configuration for DeepWiki-Open
# Ensures compatibility with packages that expect hoisted dependencies (mermaid, react-syntax-highlighter, etc.)

shamefully-hoist=true
strict-peer-dependencies=false
